<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- explicit list view definition -->
        <!--
            <record model="ir.ui.view" id="document_comparator.list">
              <field name="name">document_comparator list</field>
              <field name="model">document_comparator.document_comparator</field>
              <field name="arch" type="xml">
                <tree>
                  <field name="name"/>
                  <field name="value"/>
                  <field name="value2"/>
                </tree>
              </field>
            </record>
        -->

        <!-- actions opening views on models -->

        <record model="ir.actions.act_window"
                id="document_comparator.action_window">
            <field name="name">document_comparator window</field>
            <field name="res_model">document.comparator</field>
            <field name="view_mode">tree,form</field>
            <field name="context">{}</field>
        </record>


        <!-- server action to the one above -->
        <!--
            <record model="ir.actions.server" id="document_comparator.action_server">
              <field name="name">document_comparator server</field>
              <field name="model_id" ref="model_document_comparator_document_comparator"/>
              <field name="state">code</field>
              <field name="code">
                action = {
                  "type": "ir.actions.act_window",
                  "view_mode": "tree,form",
                  "res_model": model._name,
                }
              </field>
            </record>
        -->

        <!-- Top menu item -->

        <menuitem name="document_comparator" id="document_comparator.menu_root"
                  groups="document_comparator.groups_user"/>

        <!-- menu categories -->
        <!--
            <menuitem name="Menu 1" id="document_comparator.menu_1" parent="document_comparator.menu_root"/>
            <menuitem name="Menu 2" id="document_comparator.menu_2" parent="document_comparator.menu_root"/>
        -->
        <!-- actions -->

        <menuitem name="List" id="document_comparator.menu_1_list"
                  parent="document_comparator.menu_root"
                  action="document_comparator.action_window"/>
        <!--
            <menuitem name="Server to list" id="document_comparator" parent="document_comparator.menu_2"
                      action="document_comparator.action_server"/>
        -->
    </data>
</odoo>
