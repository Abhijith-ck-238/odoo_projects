<?xml version="1.0" encoding='UTF-8'?>
<odoo>
    <record id="view_move_form_expiry" model="ir.ui.view">
        <field name="name">stock.production.lot.inherit.form</field>
        <field name="model">stock.lot</field>
        <field name="inherit_id" ref="stock.view_production_lot_form" />
        <field name="arch" type="xml">
            <xpath expr="(//field[@name='company_id'])[2]" position="before">
                <field name="product_unreserved_qty" invisible="1"/>
                <field name="date_alert_date"/>
            </xpath>

            <xpath expr="//field[@name='product_id']" position="after">
                <field name="partner_ids" widget="many2many_tags"/>
            </xpath>
        </field>
    </record>
</odoo>